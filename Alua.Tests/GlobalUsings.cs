// LibraryVM relies on the app's global using for the MVVM toolkit; mirror just that one here so
// the linked source compiles without pulling in the app's Uno-specific global usings.
global using CommunityToolkit.Mvvm.ComponentModel;
